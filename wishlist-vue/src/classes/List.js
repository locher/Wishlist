class List {
  constructor(list) {
    this.id = list.id
    this.title = list.title
    this.description = list.description
    this.link = list.link
    this.id_user = list.id_user
    this.date_creation = list.date_creation
    this.date_modification = list.date_modification
  }
}

export default List
